# Dependencias aisladas del servidor MCP (Gestor de Proyectos TI).
# Se instalan vía `uv run --with ...` o `pip install -r requirements-mcp.txt`.
# NO contaminan requirements.txt del backend (backend_v2/requirements.txt).

# MCP SDK oficial (Model Context Protocol)
mcp>=1.0,<2.0

# Cliente HTTP asíncrono para wrappear la API REST del backend
httpx>=0.27,<1.0

# Almacenamiento seguro de credenciales en el keyring del SO
# Windows Credential Manager / macOS Keychain / Linux Secret Service
keyring>=25.0,<26.0

# JWT parsing + verificación de firma en el cliente MCP
python-jose[cryptography]>=3.3,<4.0
