# === CONFIGURACIÓN DE PRODUCCIÓN ===
# Copiar este archivo como .env en el servidor y editar los valores.

# IPs del servidor de producción (Reemplazar con la IP real del servidor Windows)
BACKEND_HOST=192.168.x.x
FRONTEND_HOST=192.168.x.x

# Puertos
BACKEND_PORT=8000
FRONTEND_PORT=80 # Nginx escuchará en el 80

# Base de datos local (Contenedor Docker)
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password_seguro_aqui
DB_NAME=project_manager

# ERP externo (Solid ERP)
ERP_DATABASE_URL=postgresql://postgres:AdminSolid2025@192.168.0.21:5432/solid

# Seguridad (IMPORTANTE: Cambiar en producción)
SECRET_KEY=clave_muy_secreta_y_larga_generada_para_produccion
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Ambiente
ENVIRONMENT=production
